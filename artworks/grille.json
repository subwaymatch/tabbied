{
  "name": "Grille",
  "slug": "grille",
  "galleryOrder": 288,
  "description": "A clean grille of lines, joints studded here and there on reseed.",
  "palette": [
    "#FBF7EE",
    "#FF4D6D",
    "#2E86AB",
    "#F5B82E",
    "#7E4A8C",
    "#2BB3A3"
  ],
  "colors": {
    "min": 2,
    "max": 6,
    "default": 6
  },
  "options": [
    {
      "id": "grid",
      "displayName": "Columns and rows",
      "type": "ButtonSelectGroup",
      "default": "10x15",
      "options": [
        "2x3",
        "4x6",
        "6x9",
        "8x12",
        "10x15"
      ],
      "replace": "${grid}"
    },
    {
      "id": "frequency",
      "displayName": "Frequency",
      "type": "Slider",
      "default": 1,
      "min": 0.2,
      "max": 1,
      "step": 0.1,
      "replace": "${shapeFrequency}"
    }
  ],
  "code": {
    "style": "--rule: ( margin: -0.5px; @random(${shapeFrequency}) { border-top: @pick(6px, 10px) solid @p(var(--color1), var(--color2), var(--color3), var(--color4), var(--color5)); } @random(${shapeFrequency}) { border-left: @pick(6px, 10px) solid @p(var(--color1), var(--color2), var(--color3), var(--color4), var(--color5)); } @random(0.18) { :after { content: ''; position: absolute; @size: 26%; left: 37%; top: 37%; border-radius: @pick(0, 50%); background: @p(var(--color1), var(--color2), var(--color3), var(--color4), var(--color5)); -webkit-transition: ease 400ms; transition: ease 400ms; } } -webkit-transition: ease 400ms; transition: ease 400ms; );",
    "doodle": ":doodle { @grid: ${grid}; @size: ${width} ${height}; overflow:hidden; text-align:center; box-sizing:border-box } :container { background: var(--color0); overflow:hidden; }"
  }
}
